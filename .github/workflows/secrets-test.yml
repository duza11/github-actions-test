name: Secrets
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    env:
      SECRET: ${{ secrets.TEST }}
    steps:
      - run: echo "$SECRET"
      - run: echo "${SECRET}"
      - run: echo "${SECRET:0:1} ${SECRET#?}"
      - run: echo "${SECRET:0:1}${SECRET#?}"
